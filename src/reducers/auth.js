const initState = {
    token: null,
    user: {}
};

export const NAME = 'auth';

export default (state = initState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}